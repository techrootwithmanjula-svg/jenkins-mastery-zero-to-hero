# Knowledge Map (Mirrors `src`)

This knowledge folder mirrors the runtime `src` structure so documentation evolves with code.

## How to Maintain

- When adding a new source file, add/update a matching knowledge note.
- Keep notes focused on intent, contracts, and edge cases (not implementation noise).
- Update docs in the same PR whenever behavior, validation, or schema changes.
