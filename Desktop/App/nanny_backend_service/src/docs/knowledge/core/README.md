# `src/core`

Cross-cutting shared layer for the entire application.

- `middleware`: request pipeline helpers (auth, role, validation, error).
- `utils`: reusable helpers (JWT, OTP, logger, response formatter).
- `constants`: shared domain constants and messages.
- `exceptions`: standard custom error types.
