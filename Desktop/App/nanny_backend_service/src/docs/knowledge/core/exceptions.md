# `src/core/exceptions`

## Purpose

Standardizes custom error shape passed to error middleware.

## Current File

- `custom-error.exception.js`

## Future Reference

- Add typed custom errors here if error semantics grow (auth errors, validation errors, conflict errors).
