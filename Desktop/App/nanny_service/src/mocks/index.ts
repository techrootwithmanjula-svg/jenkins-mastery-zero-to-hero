/**
 * Mock mode flag.
 * Set to `true` to use mock data for UI testing without a backend.
 * Set to `false` when a real API is available.
 */
export const USE_MOCK_API = true;

export { mockAppointments } from './data';
export { getMockAppointmentsForDate } from './data';
